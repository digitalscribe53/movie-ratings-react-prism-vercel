{
    "version": 2,
    "buildCommand": "cd client && npm install --include=dev && npm run build",
    "outputDirectory": "client/dist",
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/$1"
      },
      {
        "src": "/(.*)",
        "dest": "/client/dist/$1"
      }
    ],
    "env": {
      "NODE_ENV": "production"
    }
  }